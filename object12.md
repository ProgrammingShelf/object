### 다형성
추상 인터페이스에 대해 서로 다른 구현을 연결할 수 있는 능력  

#### 유니버설 다형성
- 매개변수 다형성 : 제네릭 프로그래밍, 클래스의 인스턴스 변수나 메서드의 매개변수 타입을 임의의 타입으로 선언한 후 사용하는 시점에 구체적인 타입으로 지정하는 방식
- 포함(서브타입) 다형성 : 메시지가 동일하더라도 수신 객체의 타입에 따라 수행되는 행동이 달라지는 능력
  - 자식 클래스가 부모 클래스의 서브 타입이어야 한다
 
#### 임시 다형성
- 오버로딩 다형성 : 하나의 클래스 안에 동일한 이름의 메서드가 존재하는 경우
  - 메서드의 이름을 모두 기억할 필요 없이 하나의 이름만 기억하면 된다
- 강제 다형성 : 언어가 지원하는 자동적인 타입 변환, 사용자가 직접 구현한 타입 변환을 이용해 동일한 연산자를 다양한 타입에 사용할 수 있는 것
  - 예) 자바의 '+' 의 경우 하나는 정수형이고 다른 하나는 문자열인 경우 연결 연산자로 동작한다

### 상속
상속은 코드 재사용이 아닌 클라이언트 관점에서 인스턴스들을 동일하게 행동하는 그룹으로 묶기 위해서 하는 것이다.
  
- 데이터 관점의 상속 : 부모 클래스에서 정의한 모든 데이터를 자식 클래스의 인스턴스에 자동으로 포함시킬 수 있음
  - 자식 클래스의 인스턴스 안에 부모 클래스의 인스턴스를 포함하는 것
  - 자식 클래스의 인스턴스는 자동으로 부모 클래스에서 정의한 인스턴스를 모두 변수 내부에 포함하게 됨
- 행동 관점의 상속 : 부모 클래스에서 정의한 일부 메서드 역시 자동으로 자식 클래스에 포함시킬 수 있음

### 업캐스팅과 동적 바인딩
부모 클래스와 자식 클래스에 동일한 시그니처를 가진 메서드가 존재할 경우 자식 클래스의 메서드 우선순위가 더 높다 (메시지를 수신했을 때 부모 클래스의 메서드가 아닌 자식 클래스의 메서드가 실행된다)
- 메서드 오버라이딩 : 자식 클래스 안에 상속받은 메서드와 동일한 시그니처 메서드를 재정의해서 부모 클래스의 구현을 새로운 구현으로 대체하는 것
  - 반대로, 두 메서드의 시그니처가 다를 경우 대체하지 않는다
  - 다시 말해 클라이언트는 두 메서드를 모두 호출할 수 있다
- 메서드 오버로딩 : 부모 클래스에서 정의한 메서드와 이름은 동일하지만 시그니처는 다른 메서드를 자식 클래스에 추가하는 것

- 업캐스팅 : 부모 클래스 타입으로 선언된 변수에 자식 클래스의 인스턴스를 할당하는 것
  - 업캐스팅으로 미래의 자식 클래스들도 메시지를 이해하며 확장이 가능하다
- 다운 캐스팅 : 부모 클래스의 인스턴스를 자식 클래스의 타입으로 변환하기 위해 명시적인 타입 캐스팅을 하는 것
  
- 동적 바인딩(지연 바인딩) : 선언된 변수의 타입이 아니라 메시지를 수신하는 객체의 타입에 따라 실행되는 메서드가 결정되는 것 (컴파일이 아니라 실행 시점에 적절한 메서드를 결정하는 것)
- 정적 바인딩(초기 바인딩, 컴파일 타임 바인딩) : 컴파일 타임에 호출할 함수를 결정하는 방식
  
객체 지향 언어에서는 메시지를 수신했을 때 실행될 메서드가 런타임에 결정된다
  
##### 메시지를 실행할 메서드를 결정하는 규칙
1. 메시지를 수신한 객체는 먼저 자신을 생성한 클래스에 적합한 메서드가 존재하는지 검사, 존재할 경우 탐색 종료
2. 1.에서 없었다면 부모 클래스에서 메서드 탐색, 적합한 메서드를 찾을 때까지 상속 계층을 올라감
3. 최상위 클래스에 왔어도 없으면 예외를 발생시킴

self 전송 : 현재 객체에게 메시지를 전송하는 것, 현재 객체는 self 참조가 가리키는 객체이며 처음에 메시지를 수신했던 바로 그 객체다
- 자식 클래스에서 부모 클래스 방향으로 진행되는 동적 메서드 탐색 경로를 다시 self 참조가 가리키는 원래의 자식 클래스로 이동시킨다
- 실제로 실행될 메서드를 이해하기 위해 상속 계층 전체를 훑어가며 코드를 이해해야 할 수도 있다

super 참조 : 자식 클래스에서 부모 클래스의 구현을 재사용해야 하는 경우. 지금 이 클래스의 부모 클래스에서부터 메서드 탐색을 실행하기 위해 사용. 
만약 부모 클래스에서 원하는 메서드를 찾지 못하면 더 상위의 부모 클래스로 이동하면서 메서드가 존재하는지 검사. 조상 어딘가에 메서드가 있기만 하면 실행된다.
- 부모 클래스의 메서드 호출 : 부모 클래스 안에 해당 메서드가 반드시 정의되어야 함
- 부모 클래스에서 메서드 탐색 : 조상 어딘가에 그 메서드가 정의돼 있기만 하면 됨
- 이러한 특성으로 super 참조는 super 전송이라고 부른다
  
self 참조는 어디서 탐색을 시작할 지 동적으로 결정하지만 super 전송은 항상 메시지를 전송하는 부모 클래스에서부터 시작된다.   
따라서 super 전송은 컴파일 시점에 미리 결정할 수 있다. (다만 런타임에 결정되는 경우도 있다. 스칼라의 믹스인의 경우 그렇다.)
